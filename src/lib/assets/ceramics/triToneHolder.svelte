<!--
Auto-generated by: https://github.com/threlte/threlte/tree/main/packages/gltf
Command: npx @threlte/gltf@2.0.3 /Users/danielzhu/Documents/dszhudes/static/triToneHolder.glb --types
-->

<script lang="ts">
  import type * as THREE from "three";
  import { createTransition } from "@threlte/extras";
  import type { Mesh } from "three";
  import { cubicOut } from "svelte/easing";
  import { Group } from "three";
  import {
    T,
    type Props,
    type Events,
    type Slots,
    forwardEventHandlers,
  } from "@threlte/core";
  import { useGltf } from "@threlte/extras";

  type $$Props = Props<THREE.Group>;
  type $$Events = Events<THREE.Group>;
  type $$Slots = Slots<THREE.Group> & { fallback: {}; error: { error: any } };

  export const ref = new Group();

  type GLTFResult = {
    nodes: {
      mesh_0: THREE.Mesh;
    };
    materials: {};
  };

  const scale = createTransition<Mesh>((ref) => {
    return {
      tick(t) {
        // t is [0,1] and needs to be converted to [0.5,1]
        t = 0.5 + t * 4.5;
        ref.scale.set(t, t, t);
      },
      easing: cubicOut,
      duration: 100,
    };
  });

  const gltf = useGltf<GLTFResult>("/triToneHolder.glb");
  const component = forwardEventHandlers();

  $: console.log($gltf)
</script>

<T is={ref} dispose={false} {...$$restProps} bind:this={$component}>
  {#await gltf}
    <slot name="fallback" />
  {:then gltf}
    <T.Mesh
      in={scale}
      out={scale}
      geometry={gltf.nodes.mesh_0.geometry}
      scale={5}
      position={[0, -0.85, 0]}
    >
      <T.MeshPhysicalMaterial clearcoat={1} />
    </T.Mesh>
  {:catch error}
    <slot name="error" {error} />
  {/await}

  <slot {ref} />
</T>

<!-- material={gltf.nodes.mesh_0.material} -->
